# Modules
from datetime import date

#Code Start
#Input 1 / While Loop 1
while True:
    first_name = input("What is your first name? ")
    last_name = input("What is your last name? ")
    birthdate = int(input("What year were you born?"))
    birthdate_date = date(birthdate, 1, 1)
    age = int((date.today() - birthdate_date).days/365.335)

    while True:
        if int(age) > 50:
            pcard = input("Do you have a pension card? ")
            if pcard.lower() == "yes": pcardno = int(input("Please enter your Pension Card Number"))
            break
        if int(age) < 50:
            break

#Input 2 / While Loop 2
    while True:
        status = input("Have you been here before?")
        if status.lower() == "yes":
            print("Thank you. A doctor will be with you shortly.")
            exit()
        elif status.lower() == "no":
            print("Please fill out New Patient Form")
            break
        else:
            print("Yes or No answer only")

#Input 3 / While Loop 3
    address = input("What is your current residential address?: ")

    def pencode(pcode):
        if int(age) > 50:
            print("|", "Pension Card No: ", int(pcardno))
            return answer

    print("***Are these details correct?***")
    print("Name: ", first_name, last_name)
    print("Age: ", age, "|", "Year Born: ", birthdate)
    print("Address: ", address, pencode)


    while True:
        answer = input("Yes/No?: ")
        if answer.lower() == "no":
            break
        elif answer.lower() == "yes":
            print("Thank you. A doctor will be with you shortly.")
            exit()
        else:
            print("Yes or No answers only.")
        continue
